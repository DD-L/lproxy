1. Build

    target:
        ./bin/lsslocal.exe
        ./bin/lssserver.exe
        ./bin/lkeep.exe

    Command:
        Build in linux:
        $ make -f Makefile -j 4

        Build in Cygwin:
        $ make -f Makefile.Cygwin -j 4
        
2. Run
    
    Command:
        $ ./bin/lsslocal.exe --help
        $ ./bin/lssserver.exe --help
        $ ./bin/lkeep.exe --help
